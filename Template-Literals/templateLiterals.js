// it is just about using the backtick -> ``

var a = 13;
var b = 13;
var name = 'Shohag';

console.log(`My name is ${name} and i'm ${a+b} years old`);

// we can print in newLine too using the template Literals
console.log(`My
    name is
        ${name}`);